<h1>All Ads</h1>
<% @ads.each do |ad| %>
  <ul>
		<li>			
			<%= link_to ad.name, ad %>
			<%= link_to "Delete", ad, confirm: "Are you sure?", method: :delete %>
		</li>
	</ul>
<% end %>

<br/>

<%= link_to 'New Ad', new_ad_path %>